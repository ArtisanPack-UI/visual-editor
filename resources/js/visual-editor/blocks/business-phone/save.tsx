/**
 * Business Phone — save component.
 *
 * Dynamic block: the phone number is resolved server-side from the host's
 * `ap.visualEditor.businessInfo` filter. Returning `null` from save is
 * the Gutenberg convention for dynamic blocks.
 */

export default function BusinessPhoneSave(): null {
    return null;
}
