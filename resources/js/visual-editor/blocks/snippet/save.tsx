/**
 * Snippet save.
 *
 * The block is server-rendered — the referenced snippet's tree is
 * expanded at render time by SnippetBlock's PHP renderer. The saved
 * markup is a bare comment marker via `null`.
 *
 * @since 1.4.0
 */

export default function SnippetSave() {
    return null;
}
