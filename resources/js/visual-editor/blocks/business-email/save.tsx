/**
 * Business Email — save component.
 *
 * Dynamic block: the email address is resolved server-side from the host's
 * `ap.visualEditor.businessInfo` filter. Returning `null` from save is
 * the Gutenberg convention for dynamic blocks.
 */

export default function BusinessEmailSave(): null {
    return null;
}
