/**
 * Latest Posts — constants.
 *
 * Ported verbatim from `@wordpress/block-library/src/latest-posts/constants.js`
 * (v9.43.0). Kept local so the fork does not reach into the block-library
 * package's source tree.
 */

export const MIN_EXCERPT_LENGTH = 10;
export const MAX_EXCERPT_LENGTH = 100;
export const MAX_POSTS_COLUMNS = 6;
export const DEFAULT_EXCERPT_LENGTH = 55;
