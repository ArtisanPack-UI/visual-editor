{{--
 * Template Parts CRUD Livewire View
 *
 * Headless component — no visible UI. Listens for Alpine events
 * and performs CRUD operations via Livewire, then dispatches
 * results back to the browser.
 *
 * @package    ArtisanPack_UI
 * @subpackage VisualEditor\Views\Livewire
 *
 * @since      1.0.0
 --}}

<div class="hidden" aria-hidden="true"></div>
